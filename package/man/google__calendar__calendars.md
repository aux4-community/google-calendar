#### Description

The `google calendar calendars` command group manages the calendars on the authenticated user's calendar list.

Available subcommands:

- **list** — List the calendars on the user's calendar list

#### Usage

```bash
aux4 google calendar calendars <subcommand>
```

#### Example

```bash
aux4 google calendar calendars list
```
